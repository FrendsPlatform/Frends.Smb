name: ReadFile_release

on:
  workflow_dispatch:

jobs:
  build:
    uses: FrendsPlatform/FrendsTasks/.github/workflows/release.yml@main
    with:
      workdir: Frends.Smb.ReadFile
      dotnet_version: 8.0.x
      strict_analyzers: true
    secrets:
      feed_api_key: ${{ secrets.TASKS_FEED_API_KEY }}
